{
  // https://nuxt.com/docs/guide/concepts/typescript
  // extends form (not project references) — nuxi typecheck runs vue-tsc --noEmit
  // instead of the broken -b build mode (nuxt/nuxt#34212)
  "extends": "./.nuxt/tsconfig.json"
}
